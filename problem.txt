I want to expand the scope of this optimization task a bit. Before you modify the core search engine logic, please write an automated benchmark test suite to measure the performance of our current naive search implementation. These tests must specifically measure and log the query execution times for the target words God, You, and Matthew against our text datasets so we have a clear baseline. The benchmark should run as a separate npm script (not part of `npm test`) and only record numbers to stdout — it must not assert thresholds or fail the run.

Once those benchmark tests are in place, go ahead and implement the optimized search engine using the minisearch library that is already in our package dependencies and update the existing benchmarking suite where necessary. Instead of completely replacing the old implementation, we want to give the user a choice. Please extend the initial CLI interface flow so that after selecting the files, the user can choose whether they want to run their queries using the old search engine or the new minisearch engine.

To ensure this works without breaking the application, both the original naive search engine and the new minisearch engine must expose the exact same function signatures and return the exact same data formats to the CLI. The results returned by both engines for any given query must be completely identical: both engines must perform case-insensitive whole-word matching, so searching "God" returns the same records under either engine and never matches substrings like "Godfearing" or "Godly".
